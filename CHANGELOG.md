# 更新日志

## v0.3.2 - 2026-09-02

### 新增与修复

- 桌宠右键菜单新增动态的“停止角色移动 / 允许角色移动”总开关。
- 停止移动会立即结束当前行走，并禁用手动左右行走与待机自动散步；长按拖拽仍保持可用。
- 右键移动开关与控制台“允许桌宠自主移动”双向同步，并保存到用户级配置。
- 桌宠运行时状态新增移动开关字段，补充关闭、恢复和跨窗口同步回归测试。

## v0.3.1 - 2026-09-02

### 新增

- 控制台新增 70%～130% 桌宠大小滑杆，右键菜单新增 75%、100% 和 125% 快捷档位。
- 桌宠比例和缩放后的窗口位置写入用户级配置，重启、升级后继续保留。
- 未选择可用模型、缺少 API Key 或模型地址时，在快捷输入中显示明确提示并可一键跳转配置。

### 修复与体验

- 修复鼠标悬停动作仅播放少量帧便结束的问题，悬停和等待输入动作现在持续循环。
- 快捷输入展开时不再缩小角色，输入框与回复气泡改为半透明设计。
- 缩放时保持桌宠底边中心锚点，并同步调整窗口、点击区域、拖拽边界与屏幕边界判断。
- 修复等待配置、打开快捷输入后动作停止的问题。

### 验证

- 补充缩放持久化、悬停循环、未配置模型引导、开发版与安装包窗口几何回归。

## v0.3.0 - 2026-09-02

### 新增

- 单击桌宠后在角色下方展开快捷输入，不再直接弹出完整聊天窗口。
- 流式回复显示为桌宠头顶语言气泡，默认保留 10 秒。
- 回复气泡停留时间支持在桌宠右键菜单和控制台中调整，并持久化到用户配置。
- 右键“打开完整对话”保留原有聊天历史、停止回答、重试与清空能力。

### 动作与体验

- 等待模型首个输出时使用角色包的“等待”动作，开始输出后切换到“回答”动作。
- 快捷输入与回复气泡均复用固定尺寸透明桌宠窗口，不拉伸窗口，不影响长按拖拽几何尺寸。
- 完整对话完成后不再按旧气泡计时自动隐藏，由用户主动关闭或失焦收起。

### 验证

- 新增快捷输入、流式回复气泡、等待/回答动作、完整对话恢复和气泡时长持久化回归。

## v0.2.2 - 2026-09-02

### 新增

- “允许桌宠自主移动”总开关与“待机自动散步”子开关。
- 右键“展示动作”子菜单，动态列出当前角色包的全部动作。
- 支持从桌宠窗口安全触发角色动作预览。

### 行为

- 关闭自主移动后会停止自动散步，并禁用右键手动左右位移。
- 关闭自主移动不会禁用拖拽，用户仍可自由调整桌宠位置。
- 移动设置会保存到用户级配置，并在升级或重启后恢复。

## v0.2.1 - 2026-09-02

首个 GitHub 公开版本。

### 新增

- Hatch-Pet WebP 精灵角色包扫描、解析和完整动作库。
- Rem `8×9` 角色包适配及逐行透明空帧检测。
- 左右移动状态与真实窗口水平位移，支持待机自动散步。
- OpenAI-compatible 流式 Chat、停止回答、历史恢复和错误处理。
- Windows DPAPI API Key 安全存储与模型真实连接测试。
- 自定义安装路径、便携版和标准卸载程序。

### 优化

- 修复长按拖拽后窗口边距和尺寸异常。
- 调慢精灵动画与桌宠移动速度。
- 优化控制台、角色动作库和自适应对话气泡 UI。
- 安装、升级和卸载时保留用户级模型配置、角色包与聊天数据。

### 验证

- 角色包扫描、前端构建、桌面交互、打包程序和配置持久化测试通过。
- `npm audit --audit-level=high`：0 个漏洞。
