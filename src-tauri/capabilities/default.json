{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the desktop pet console",
  "windows": ["console"],
  "permissions": ["core:default", "opener:default"]
}
